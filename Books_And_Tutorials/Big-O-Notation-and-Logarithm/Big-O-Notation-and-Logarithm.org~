#+title: Big 0 notiation and Logarithm

* Big O Notation.
- Used to show the time or space complexity in a _worst case scenario_ .

** O( 1 )
- Irrespective of the size of the data structure the time complexity is always 1.
  Example:

  #+BEGIN_SRC cpp
  int getFirstElement( std::vector<int> list )
  {
      return list[0];
  }
  #+END_SRC

  #+BEGIN_SRC cpp
  /* get value from hash table. */
  auto value = hash_table[ key ];
  #+END_SRC
** O( N )
** O( N^2 )
** O( 2^N )

* Logarithm
