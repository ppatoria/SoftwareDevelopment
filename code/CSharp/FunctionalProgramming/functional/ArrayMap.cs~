using System;
using System.Linq;

class ArrayMap
{

	public static void Main()
	{
		var a = new[] { "a", "b", "c", "d" };
		a = a.Select(p => p + "!").ToArray();
		foreach(var item in a) Console.WriteLine(item);	
	}
}
